const { start } = require('./start');
const { debug } = require('./debug');
const { stat } = require('./stat');
const { gpt } = require('./gpt');

module.exports = {
    start,
    debug,
    stat,
    gpt,
}
